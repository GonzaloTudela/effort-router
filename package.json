{
  "name": "abn-effort-router",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:check": "node scripts/build.mjs --check",
    "generate": "node scripts/generate.mjs",
    "generate:check": "node scripts/generate.mjs --check",
    "test": "node --test",
    "verify": "npm run generate:check && npm run build:check && npm test"
  },
  "devDependencies": {
    "ajv": "8.17.1",
    "esbuild": "0.25.6"
  },
  "dependencies": {
    "php-parser": "3.7.0"
  }
}
